# PyCharm 自动生成的配置文件（绝对不要上传）
.idea/
*.iml
*.iws
*.ipr

# Python 缓存文件
__pycache__/
*.py[cod]
*.so
.pytest_cache/
.coverage
htmlcov/

# 虚拟环境（几百MB，绝对不要上传）
venv/
env/
ENV/

# PyInstaller 打包生成的文件（exe在这里，绝对不要上传到代码仓库）
build/
dist/
*.spec

# 系统垃圾文件
.DS_Store
Thumbs.db
*.log

# AI 代理工作目录与本地工具数据（体积大、仅本机有效，不要上传）
.claude/worktrees/
.workbuddy-ai/

#🔔 划重点：不用管这些，直接往下看介绍就好！